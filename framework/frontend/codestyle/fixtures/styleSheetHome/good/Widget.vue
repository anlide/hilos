<!-- The look-alike: an SFC that styles itself with Bootstrap classes and carries
     no <style> block, so STYLE-SHEET-HOME must stay silent on it. The word the
     rule looks for appears in the template as ordinary text. -->
<template>
  <div class="p-3 border rounded">
    <p class="mb-0">A widget with no style block of its own.</p>
  </div>
</template>
