// TODO: decide whether to evolve this into a proper base (like backend DbItem) or remove inheritance in consumers
/**
 * Base class for domain objects transferred over WebSocket.
 * Currently empty — exists so demo entity classes can share a common ancestor.
 */
export abstract class DomainObject {
}
