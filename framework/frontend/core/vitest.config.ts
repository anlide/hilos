// The @hilos/core test project: framework-free logic, tested with no browser
// environment at all.
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    name: 'core',
  },
})
