# Toy Document

A document describes structure, so a bare path in it is prose. It names
`catalog/ghost.md` the way the real docs name a file inside another root — the
target does not exist, and the guard must still stay silent.

A markdown link is different: it was written to be followed, so a stale one is a
break wherever it sits, including [here](../catalog/missing.md).
