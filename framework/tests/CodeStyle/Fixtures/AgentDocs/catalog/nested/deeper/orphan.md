# Nested Orphan Rule

A catalog file two folders down that no wrapper points at, and which says nothing
about why. The same case as the flat orphan, hidden behind a directory — the walk
around the mechanism that recursion exists to close.

Two levels deep on purpose: one level would also be found by a pattern matching a
single directory, and this file has to fail that substitution too.
