# Routed And Declined

Routed from: none — kept here to prove a refusal that outlived its truth is
reported.

A wrapper does route to this file, so the line above is stale. Left unreported it
would go on telling the next reader that the file is deliberately unreachable,
which is no longer so.
