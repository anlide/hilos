{
  "name": "chat-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:realtime": "playwright test --grep @realtime",
    "test:non-realtime": "playwright test --grep-invert @realtime",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2"
  }
}
