# Test environment configuration
# Copy to .env.test and adjust if needed (e.g. DB_HOST=localhost when running PHPUnit from host)

APP_ENV=test
DB_HOST=mysql-test
DB_PORT=3306
DB_DATABASE=hilos-demo-chat-test
DB_USERNAME=hilos
DB_PASSWORD=hilos_pass
