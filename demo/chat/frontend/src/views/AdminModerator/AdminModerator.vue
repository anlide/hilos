<!-- The chat moderation admin page (PAGE_ADMIN_MODERATOR) at /hilos/admin_moderator:
an application-specific admin area, reached from the dashboard's "Chat
administration" section. Placeholder for now — the backend is ready
(AdminModeratorPage + the moderator prompt-pieces table); the real table and its
actions land in a later step. Bootstrap classes only (styling-rules.md). -->
<template>
  <section data-id="admin-moderator-view">
    <div class="d-flex flex-column gap-1 mb-4">
      <h1 class="h4 mb-0">Moderation</h1>
      <p class="mb-0 text-body-secondary">
        Moderator prompt pieces and rules. Placeholder — the backend is ready;
        the table lands in a later step.
      </p>
    </div>
  </section>
</template>
