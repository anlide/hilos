# Build artifacts and the npm-managed lockfile are not formatted by Prettier.
dist/
dist-prerender/
package-lock.json

# Markdown docs are hand-authored to the repo's documentation style, not Prettier's.
**/*.md
