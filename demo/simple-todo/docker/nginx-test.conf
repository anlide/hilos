# Static serving of the built frontend artifact for the e2e test stack.
# SPA fallback: unknown paths resolve to index.html so client-side routes load.
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
