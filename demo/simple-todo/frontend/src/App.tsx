// Blank root view — the step 5.1 conformance shell: a React app that builds
// and serves through the docker stack. Real views land on top of this from
// step 7, tracking each core capability as it lands.
export default function App() {
  return <main data-id="app-root">Hilos simple-todo (React)</main>
}
