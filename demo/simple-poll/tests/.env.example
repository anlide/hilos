# Test environment configuration
# Copy to tests/.env (`composer run setup-env` does this) and adjust if needed
# (e.g. DB_HOST=localhost and DB_PORT=33066 when running PHPUnit from host)

APP_ENV=test
DB_HOST=poll-mysql-test
DB_PORT=3306
DB_DATABASE=hilos-demo-simple-poll-test
DB_USERNAME=hilos
DB_PASSWORD=hilos_pass
