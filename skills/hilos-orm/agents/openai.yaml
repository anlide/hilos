interface:
  display_name: "Hilos ORM"
  short_description: "Hilos::$db, DbContext, collections, actions, and migrations"
  default_prompt: "Use $hilos-orm to implement a database-backed Hilos change."
