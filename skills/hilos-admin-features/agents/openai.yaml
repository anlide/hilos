interface:
  display_name: "Hilos Admin Features"
  short_description: "Graduate or build a Hilos admin feature (table+page+actions)"
  default_prompt: "Use $hilos-admin-features for admin pages, tables, and their framework/project split in Hilos, or when an admin action changes an entity the page does not own and you need to know what closes it."
