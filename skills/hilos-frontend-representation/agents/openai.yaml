interface:
  display_name: "Hilos Frontend Representation"
  short_description: "Browser DB/RT payload contracts"
  default_prompt: "Use $hilos-frontend-representation to shape Hilos browser payloads with BrowserContext rows, typed frontend state payloads, table rows, and legacy DB/RT serialization boundaries."
